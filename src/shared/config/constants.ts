/** Used by another flow to pre-fill a public location. */
export const DEFAULT_COOK_LOCATION = {
	publicLocation: "Buenaventura, Valle del Cauca",
	latitude: 3.8833,
	longitude: -77.0319,
} as const;
